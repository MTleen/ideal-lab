---
name: ideal-test-case
description: Use when P6 plan review is completed and test case generation is needed. Extracts acceptance criteria and generates functional, boundary, and exception test cases.
agents: [qa]
---

# ideal-test-case（P7 测试用例生成）

## 角色定位

Phase Skill — **执行协调者**。

职责：
1. 加载所需上下文（P1 需求 + P5 编码计划）
2. 调度子智能体生成测试用例
3. 将产物写入文件系统
4. 返回执行摘要

**不负责**：更新 flow state、验证前置条件、协调评审。

---

## 输入

| 来源 | 内容 |
|------|------|
| P1-需求文档.md | 验收标准、功能需求 |
| P5-编码计划.md | 任务清单、功能点映射 |

---

## 输出

| 文件 | 路径 |
|------|------|
| P7-测试用例.md | `docs/迭代/{需求名称}/P7-测试用例.md` |

---

## 子智能体调度

| 调用时机 | 子智能体 | 任务 |
|----------|----------|------|
| 验收标准提取 | `qa` | 从 P1 中提取验收标准作为用例基础 |
| 功能用例生成 | `qa` | 覆盖主流程和分支流程 |
| 边界用例生成 | `qa` | 输入边界、状态边界、流程边界 |
| 异常用例生成 | `qa` | 错误输入、异常状态、外部错误 |

---

## 测试用例格式

```markdown
## TC-{type}-{id}: {用例名称}

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-{type}-{id} |
| **用例名称** | {名称} |
| **优先级** | P0 / P1 / P2 |
| **前置条件** | {测试前需满足的条件} |
| **来源** | P1-需求文档.md #{章节} / P5-编码计划.md T{xxx} |

**测试步骤**:
1. {步骤1}
2. {步骤2}

**预期结果**:
{期望的输出或状态}
```

**类型前缀**：`F` = 功能测试，`B` = 边界测试，`E` = 异常测试

---

## 执行流程

```
Step 1: 加载上下文
  ├─ 读取 P1-需求文档.md（提取验收标准、功能需求）
  └─ 读取 P5-编码计划.md（提取任务和功能点）

Step 2: 验收标准提取
  └─ Task(qa) → 将验收标准映射为可测试用例

Step 3: 功能用例生成
  └─ Task(qa) → 每个功能点至少一个正向用例，覆盖主流程

Step 4: 边界用例生成
  └─ Task(qa) → 输入/状态/流程边界用例

Step 5: 异常用例生成
  └─ Task(qa) → 错误输入、异常状态、外部错误用例

Step 6: 覆盖率统计
  └─ 统计各类型用例数量，计算覆盖率

Step 7: 写入产物
  └─ 写入 P7-测试用例.md

Step 8: 返回摘要
  └─ 报告：用例数量、覆盖率、优先级分布
```

---

## 覆盖率目标

| 类型 | 占比 | 说明 |
|------|------|------|
| 功能测试 (F) | 50-60% | 验证正常功能流程 |
| 边界测试 (B) | 20-30% | 验证输入/状态边界 |
| 异常测试 (E) | 15-20% | 验证错误处理 |

---

## 质量检查清单

产物写入前必须验证：

- [ ] 每个验收标准至少有一个对应用例
- [ ] 包含功能、边界、异常三类用例
- [ ] 用例可独立执行（无执行顺序依赖）
- [ ] 预期结果明确（可判断 PASS/FAIL）
- [ ] 前置条件清晰
- [ ] 用例编号连续无重复
- [ ] P0 用例全部为正向功能用例

---

## 返回格式

```markdown
## P7 测试用例 — 执行摘要

### 产物
- 文件：docs/迭代/{需求名称}/P7-测试用例.md

### 用例统计
| 类型 | 数量 | 占比 |
|------|------|------|
| 功能测试 (F) | {N} | {X}% |
| 边界测试 (B) | {N} | {X}% |
| 异常测试 (E) | {N} | {X}% |
| **总计** | {N} | 100% |

### 优先级分布
- P0（核心，必须通过）：{N} 个
- P1（重要，应该通过）：{N} 个
- P2（次要，可延后）：{N} 个

### 验收标准覆盖
- 已覆盖：{N} / {总验收标准数}
- 覆盖率：{X}%
```

---

*Phase Skill v2.0 — 精简为执行协调者角色*
